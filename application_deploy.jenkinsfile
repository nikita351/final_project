pipeline {
  agent any
  stages{
    stage('Check git repo') {
      steps {
        checkout scm
      }
    }
    stage('terraform') {
      steps {
        sh 'cd terraform'  
        sh 'terraform apply'
      }
    }
  }
}