pipeline {
  agent any
  stages{
    stage('Check git repo') {
      steps {
        checkout scm
      }
    }
    stage('terraform') {
      steps {
        sh './terraform/terraform apply -auto-approve -no-color'
      }
    }
  }
}