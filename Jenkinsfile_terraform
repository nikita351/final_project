pipeline {
  agent any
  stages{
    stage('checkout') {
      steps {
        checkout scm
      }
    }
    stage('terraform') {
      steps {
        sh './terraformw plan -auto-approve -no-color'
      }
    }
  }
  post {
    always {
      cleanWs()
    }
  }
}