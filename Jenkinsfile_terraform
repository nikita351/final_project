pipeline {
  agent any
  stages{
    stage('checkout') {
      steps {
        checkout scm
      }
    }
    stage('terraform') {
      steps {
        sh './terraform terraform plan -auto-approve -no-color'
      }
    }
  }
  post {
    always {
      cleanWs()
    }
  }
}